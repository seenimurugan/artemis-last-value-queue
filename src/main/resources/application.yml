spring:
  artemis:
    host: localhost
    user: capp
    password: admin
    mode: native
    port: 61616
    embedded:
      enabled: false
spike:
  event-publisher: ${event-publisher:false}
  customer-publisher: ${customer-publisher:false}
  event-consumer: ${event-consumer:false}
  customer-consumer: ${customer-consumer:false}
  queue-name:
    event-update: capp.queue.eventupdate
    customer-update: capp.queue.customerupdate